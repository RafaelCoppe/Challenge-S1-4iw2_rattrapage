<div class="mb-4">
    <label for="{{ id }}" class="block text-gray-700 text-sm font-bold mb-2">{{ label }}</label>
    <input type="{{ type }}" id="{{ id }}" name="{{ name }}" class="shadow appearance-none border rounded-md w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
</div>
