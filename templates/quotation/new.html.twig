{% extends 'base.html.twig' %}

{% block title %}Nouveau devis{% endblock %}

{% block body %}
    <h1>Nouveau devis</h1>
    <a href="{{ path('quotation_index') }}" class="btn btn-primary">Retour aux devis</a>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Créer le devis</button>
    {{ form_end(form) }}
{% endblock %}
