{% extends 'base.html.twig' %}

{% block title %}Édition du devis | {{ quotation.ref }}{% endblock %}

{% block body %}
    <h1>Édition du devis : {{ quotation.ref }}</h1>
    <a href="{{ path('quotation_index') }}" class="btn btn-primary">Retour aux devis</a>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
