{% extends 'base.html.twig' %}

{% block title %}Édition du devis{{ quotation.id }}{% endblock %}

{% block body %}
    <h1>Édition du devis {{ quotation.id }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
